# Local development only (Vite exposes VITE_* vars to the browser).
# In production the app proxies Gemini through /api/gemini, so set
# GEMINI_API_KEY in the Vercel project settings instead of committing it.
VITE_GEMINI_API_KEY=your-gemini-api-key
